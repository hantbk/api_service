apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  type: NodePort
  ports:
  - port: 9000
    targetPort: 9000
    nodePort: {{ .Values.service.nodePort }}
  selector:
    app: {{ .Chart.Name }}